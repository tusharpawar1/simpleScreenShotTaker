Steps
1. compile source files and generate jar
2. create scripts directory and place all the scripts( batterySaver.VBS, and startup.cmd)
3. place jar in "JarFolder" is folder, change the startup.cmd accordingly.
3. jarname is "jar file name", change the startup.cmd accordingly.
4. "outputDir" is output dir where you want you files to be created, change the startup.cmd accordingly.
5. change the startup.cmd give timeinterval
6. create task scheduler job, and invoke VBS scripts, with trigger as startup